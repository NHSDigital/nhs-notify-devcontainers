# Loaded

Environment variables to control post create / post start:

```json
{
  "containerEnv": {
    "UPDATEFROMTEMPLATE": "true",
    "SHOWWELCOME": "true",
    "MAKECONFIG": "true",
    "GITHUBMONITOR": "true"
  }
}
```
